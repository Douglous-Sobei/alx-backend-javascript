/**
 * Returns a Promise that represents a response from an API.
 * @return {Promise} A Promise that resolves to the API response.
*/
export default function getResponseFromAPI() {
  return new Promise(() => {});
}
